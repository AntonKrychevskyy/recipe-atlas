{
  "scripts": {
    "install": "npm --prefix BE install && npm --prefix FE install",
    "start": "concurrently \"npm --prefix BE start\" \"npm --prefix FE run preview\"",
    "dev": "concurrently \"npm --prefix BE run dev\" \"npm --prefix FE run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
